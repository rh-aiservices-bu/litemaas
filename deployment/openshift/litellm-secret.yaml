apiVersion: v1
kind: Secret
metadata:
  name: litellm-secret
  labels:
    app: litellm
    component: ai-proxy
type: Opaque
stringData:
# Database Configuration - separate database for LiteLLM
  database-url: postgresql://litemaas_admin:change-me-please@postgres:5432/litellm_db
# LiteLLM Configuration
  master-key: sk-change-me-master-key
  ui-username: admin
  ui-password: change-me-ui-password
