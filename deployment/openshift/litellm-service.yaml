apiVersion: v1
kind: Service
metadata:
  name: litellm
  labels:
    app: litellm
    component: ai-proxy
spec:
  selector:
    app: litellm
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
