{{- if and (eq .Values.global.platform "openshift") .Values.route.enabled .Values.route.litellm.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "litemaas.litellm.fullname" . }}
  labels:
    {{- include "litemaas.componentLabels" (dict "context" . "component" "ai-proxy") | nindent 4 }}
  {{- with .Values.route.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.route.litellm.host }}
  host: {{ .Values.route.litellm.host | quote }}
  {{- end }}
  to:
    kind: Service
    name: {{ include "litemaas.litellm.fullname" . }}
    weight: 100
  port:
    targetPort: http
  tls:
    termination: {{ .Values.route.litellm.termination }}
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
{{- end }}
