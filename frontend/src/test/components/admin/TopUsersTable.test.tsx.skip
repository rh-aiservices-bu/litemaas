/**
 * IMPORTANT: TopUsersTable component tests are SKIPPED due to component design issue
 *
 * **Issue**: The component uses `dangerouslySetInnerHTML` in table headers (lines 216-223):
 * ```typescript
 * <Th
 *   modifier="nowrap"
 *   dangerouslySetInnerHTML={{ __html: t('adminUsage.tableHeaders.promptTokens') }}
 * />
 * ```
 *
 * **Problem**: JSDOM does not support dangerouslySetInnerHTML inside table elements due to
 * strict DOM hierarchy rules. This causes: `HierarchyRequestError: The operation would yield an incorrect node tree.`
 *
 * **Solution**: The component needs to be refactored to NOT use dangerouslySetInnerHTML in table headers.
 * Instead, use regular text content or proper React elements.
 *
 * **Recommended Fix**:
 * ```typescript
 * <Th modifier="nowrap">
 *   {t('adminUsage.tableHeaders.promptTokens').replace(/<br\/?>/g, ' ')}
 * </Th>
 * ```
 *
 * **Tests Prepared**: 20 tests covering loading state, empty state, table rendering, data formatting,
 * export functionality, expand functionality, and user display are ready in this file but cannot run
 * until the component is fixed.
 *
 * **Next Steps**:
 * 1. Refactor Top UsersTable component to remove dangerouslySetInnerHTML from table headers
 * 2. Rename this file from `.tsx.skip` to `.tsx`
 * 3. Run tests: `npm test -- TopUsersTable.test.tsx`
 *
 * **Related**: See /docs/development/pf6-guide/testing-patterns/ for PatternFly 6 testing best practices
 */

// Test file content is available in the git history (commit before .skip rename)
// Search for: "TopUsersTable.test.tsx" in git log
